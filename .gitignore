# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.eggs/

# uv / virtualenvs (skill venvs are rebuilt via scripts/setup.sh)
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
*.swp

# Skill fetched host binaries — rebuilt via scripts/setup.sh; keep the .gitkeep placeholder
seed/*/bin/*
!seed/*/bin/.gitkeep
